// APIと通信するような場合はこのファイルを使ってDBとClientの値を同期する

// import { Todo, Todos } from "../domain/models";
// import { store } from "../../src/store";

// export function addAndSync(todos: Todos.Model, newValue: Todo.Values) {
//   const newTodo = Todo.factory(newValue);
//   Todos.add(store.todos, newTodo);
// }

// removeAndSync

// toggleAndSync

// editAndSync
